import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Driver · Verify' }

export default function DriverVerifyPage() {
  return (
    <PlaceholderCard
      title="Photo verification"
      hint="Random prompts during active sessions. Vehicle + selfie within 90 s."
    />
  )
}
