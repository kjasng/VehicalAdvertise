import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Driver · Profile' }

export default function DriverProfilePage() {
  return (
    <PlaceholderCard title="Profile" hint="KYC (CCCD), vehicles, bank account for SePay payouts." />
  )
}
