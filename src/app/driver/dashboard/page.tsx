import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Driver · Home' }

export default function DriverDashboardPage() {
  return (
    <PlaceholderCard
      title="Welcome, driver"
      hint="Today's km, status, and tracking start/stop will live here."
    />
  )
}
