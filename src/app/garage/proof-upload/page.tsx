import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Garage · Proof upload' }

export default function GarageProofUploadPage() {
  return (
    <PlaceholderCard
      title="Proof upload"
      hint="Capture install or removal photo. Submits to admin review."
    />
  )
}
