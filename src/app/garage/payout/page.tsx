import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Garage · Payout' }

export default function GaragePayoutPage() {
  return <PlaceholderCard title="Payout" hint="Weekly payout invoices for completed installs." />
}
