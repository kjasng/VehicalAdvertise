import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Garage · Dashboard' }

export default function GarageDashboardPage() {
  return <PlaceholderCard title="Dashboard" hint="Today's install queue + earned this week." />
}
