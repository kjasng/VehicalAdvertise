import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Garage · Installs' }

export default function GarageInstallsPage() {
  return (
    <PlaceholderCard
      title="Installs"
      hint="Assigned install and removal orders. Tap one to upload proof."
    />
  )
}
