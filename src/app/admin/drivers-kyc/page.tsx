import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Admin · Drivers KYC' }

export default function AdminDriversKycPage() {
  return (
    <PlaceholderCard
      title="Drivers KYC"
      hint="Pending CCCD photos, selfies, vehicle registrations awaiting review."
    />
  )
}
