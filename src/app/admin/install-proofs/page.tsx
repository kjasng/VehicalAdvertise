import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Admin · Install proofs' }

export default function AdminInstallProofsPage() {
  return (
    <PlaceholderCard
      title="Install proofs"
      hint="Garage-uploaded install and removal photos awaiting review."
    />
  )
}
