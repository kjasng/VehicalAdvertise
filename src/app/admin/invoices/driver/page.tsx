import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Admin · Driver invoices' }

export default function AdminDriverInvoicesPage() {
  return (
    <PlaceholderCard
      title="Driver invoices"
      hint="Weekly payouts. Status filters, retry-failed action."
    />
  )
}
