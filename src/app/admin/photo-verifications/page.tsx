import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Admin · Photo verifications' }

export default function AdminPhotoVerificationsPage() {
  return (
    <PlaceholderCard
      title="Photo verifications"
      hint="Periodic driver photo prompts (EXIF / GPS cross-checked) awaiting human review."
    />
  )
}
