import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Admin · Reports' }

export default function AdminReportsPage() {
  return (
    <PlaceholderCard
      title="Reports"
      hint="Fraud KPIs, payout totals, cohort retention, audit log search."
    />
  )
}
