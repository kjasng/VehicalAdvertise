import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Partner · Dashboard' }

export default function PartnerDashboardPage() {
  return (
    <PlaceholderCard title="Dashboard" hint="Live spend, km this week, QR scans, top districts." />
  )
}
