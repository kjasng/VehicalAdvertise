import { PlaceholderCard } from '@/components/shared/role-nav'

export const metadata = { title: 'Partner · Billing' }

export default function PartnerBillingPage() {
  return (
    <PlaceholderCard
      title="Billing"
      hint="Top up via SePay (VietQR). View ledger, charges, and invoices."
    />
  )
}
